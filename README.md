# TrojanHorseWindowsAdmin
Doesn't bypass admin password, requires a real admin to run.
# Admin backdoor
The program needs to be run once by a (real) admin user, and then keeps a "foot in the door" to that original admin access.
